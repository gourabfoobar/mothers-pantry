import SwiftUI

/// Temporary placeholder — replaced screen by screen starting at milestone 6.
struct ScaffoldPlaceholderView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("母").font(.system(size: 40, weight: .bold, design: .serif))
            Text("Mother's Pantry — scaffold").font(.system(.body, design: .monospaced))
        }
    }
}
